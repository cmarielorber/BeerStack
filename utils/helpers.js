module.exports = {
  check_cooler: (beer_id, cooler_list) => {
    console.log(beer_id);
    console.log(cooler_list);
    console.log('------------');
    // Format date as MM/DD/YYYY
    if (cooler.includes(beer_id)) {
      return true
    } else return false
  }
};