

  <article id="searchBrew">
    <card class="border-top col-sm-12 d-flex justify-content-center">
     <div class="col-md-10" id="searchBrewStyle">
          <!-- inside card -->
     <div class="mt-3 row g-0 rounded overflow-hidden flex-md-row mb-4 shadow-sm h-md-250 position-relative">
     <div class="text-center">
      <h2>BEER LIST</h2>
  <div>
 
<div class="dropdown">
  <button onclick="myFunction()" class="dropbtn">Filter by:</button>
  <div id="myDropdown" class="dropdown-content">
    <a href="#home">Name</a>
    <a href="#about">Likes</a>
  </div>
</div>

  {{#each beers as |beers| }}
    <a class="list-item flex-row justify-space-between">
      <ul>
      {{ name }}
      </ul>
    </a>
  {{/each}}

    <button class="btn btn-primary">we need a button?</button>
  </p>
  </div>